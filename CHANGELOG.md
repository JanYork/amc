# Changelog

Release Please owns this changelog and will add release entries from conventional
commits.

## Unreleased

- Add polished README hero and interface showcase artwork generated through a headless Codex workflow.
- Add interactive startup reconciliation across `.agent`, `.agents`, Claude, Pi, and Codex Skill roots with atomic no-copy adoption, independent Pi/Codex links, conflict preservation, and crash recovery.
- Distinguish shared effective discovery from disabled provider-link state, preserve parked links during reconciliation, self-heal duplicate AMC-owned active/parked links, and support the valid `prototype` Skill name.
- Add read-only and explicitly authorized `amc reconcile` headless workflows, including reversible canonical-preserving repair of invalid shared/provider entries.
- Add skills.sh and validated public GitHub Skill discovery.
- Add GitHub authentication through official `gh` OAuth or owner-only stdin Token storage, with API-host-only Authorization, rate-limit status, and platform-specific guidance when `gh` is missing.
- Present the skills.sh all-time leaderboard by default, incrementally load subsequent pages near the list edge, and show Marketplace results in a theme-consistent responsive table with canonical installation status and lazy GitHub descriptions and provenance details.
- Isolate remote symlink/submodule rejection to the containing Skill directory without ever following or installing unsupported entries.
- Resolve duplicate hidden plugin mirrors consistently for both descriptions and installation by preferring exact conventional Skill paths.
- Add read-only content-level update checks for one or all applied canonical Skills, with persistent TUI explanations, a status legend, and shared repository scans.
- Add provenance-aware Skill installation and local-drift-safe upgrades.
- Add second-confirmed, journaled permanent Skill deletion across AMC-owned copies.
